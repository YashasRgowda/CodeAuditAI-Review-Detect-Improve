# ============================================================================
# MODELS/ — SQLAlchemy Database Models
# ============================================================================
# Defines all database table structures (ORM models):
#   - User          → users table (GitHub OAuth users)
#   - Repository    → repositories table (tracked GitHub repos)
#   - Analysis      → analysis_results table (commit analysis results)
#   - PullRequest   → pull_requests table (tracked PRs)
#   - PRAnalysis    → pr_analysis_results table (PR analysis results)
# ============================================================================
